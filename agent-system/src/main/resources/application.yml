spring:
  application:
    name: ai-agent
  profiles:
    active: dev  # 默认激活开发环境

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    com.agent: INFO
    org.springframework: INFO


